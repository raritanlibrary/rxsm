{
  "name": "rxsm",
  "version": "1.0.0",
  "description": "Remote execution scripts for server maintenance.",
  "type": "module",
  "scripts": {
    "report": "node --experimental-json-modules report.js",
    "service": "bash ./service.sh",
    "version": "bash ./version.sh"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "puppeteer": "^12.0.1"
  }
}
